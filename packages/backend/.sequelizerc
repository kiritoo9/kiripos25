const path = require("path");

module.exports = {
    "config": path.resolve("src", "infras", "database", "config.ts"),
    "models-path": path.resolve("src", "applications", "models"),
    "migrations-path": path.resolve("src", "infras", "database", "migrations"),
    "seeders-path": path.resolve("src", "infras", "database", "seeders"),
}